// Literal Types
// Literal types allow you to specify the exact value a string, number, or boolean must have.

// Not only is quantity a number, but it can only be 50.
let quantity: 50 = 50;

let quantity2: 50 | 100 = 50;

let metric: "cm" | "inch" = "cm";
